# STEGNOGRAPHY
Stegnography on a colour image
Takes no of bits and colour plane into which info is to be hidden.
Returns back a file having info after decrypting the info from image.
Uses a symbol file for encryption
